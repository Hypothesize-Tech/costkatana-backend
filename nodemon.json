{
    "watch": ["src"],
    "ext": "ts",
    "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts"],
    "exec": "node --max-old-space-size=4096 --expose-gc -r ts-node/register -r tsconfig-paths/register ./src/server.ts",
    "env": {
      "NODE_ENV": "development"
    }
}