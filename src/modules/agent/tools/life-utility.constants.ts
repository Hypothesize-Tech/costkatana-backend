/**
 * Message shown when image-based reverse search is attempted (not implemented).
 * Used only when an image input path is detected; text-based reverse search has no note.
 */
export const LIFE_UTILITY_IMAGE_UPLOAD_NOTE =
  'Image-based identification is not supported. Provide a text description instead.';
